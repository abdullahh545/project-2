

  <a href="/lists">Back</a>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Task - My Bucket List</title>
  <link rel="stylesheet" href="/edit2.css">
</head>
<body>
  <%- include('../partials/navbar') %>

  <div class="task-edit-container">
    <div class="task-edit-form">
      <h1>Edit Task</h1>
      
      <form action="/tasks/<%= task._id %>" method="POST">
        <div class="form-group">
          <label for="title">Title:</label>
          <input type="text" name="title" id="title" value="<%= task.title %>" required>
        </div>

        <div class="form-group">
          <label for="category">Category:</label>
          <input type="text" name="category" id="category" value="<%= task.category %>" required>
        </div>

        <div class="form-group">
          <label for="description">Description:</label>
          <textarea name="description" id="description" required><%= task.description %></textarea>
        </div>

        <div class="checkbox-group">
          <input type="checkbox" name="completed" id="completed" <%= task.completed ? 'checked' : '' %>>
          <label for="completed">Completed</label>
        </div>

        <div class="form-actions">
          <button type="submit">Update Task</button>
        </div>
      </form>

      <form action="/tasks/<%= task._id %>/delete" method="POST" class="delete-form">
        <button type="submit">Delete Task</button>
      </form>

      <div class="back-link">
        <a href="/lists">← Back to Lists</a>
      </div>
    </div>
  </div>
</body>
</html>
